/**
 * @deprecated Import from '@/backend/domains/session' instead.
 * This re-export shim will be removed in Phase 9 (Import Rewiring).
 */

export type {
  BuildPromptInput,
  BuildPromptResult,
} from '@/backend/domains/session/lifecycle/session.prompt-builder';
export {
  SessionPromptBuilder,
  sessionPromptBuilder,
} from '@/backend/domains/session/lifecycle/session.prompt-builder';
