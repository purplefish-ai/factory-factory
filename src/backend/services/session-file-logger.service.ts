/**
 * @deprecated Import from '@/backend/domains/session' instead.
 * This re-export shim will be removed in Phase 9 (Import Rewiring).
 */
export {
  extractLogSummary,
  SessionFileLogger,
  sessionFileLogger,
} from '@/backend/domains/session/logging/session-file-logger.service';
